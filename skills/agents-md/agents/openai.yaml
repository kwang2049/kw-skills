interface:
  display_name: "AGENTS.md Manager"
  short_description: "Create, edit, or memorize AGENTS.md rules."
  default_prompt: "Use $agents-md to create or edit AGENTS.md, or add memorize plus a rule to store it in the right section."
