interface:
  display_name: "Implement"
  short_description: "Propose, refine, then implement."
  default_prompt: "Use $implement to turn this idea into an implementation proposal, refine it with feedback, then implement after approval."
